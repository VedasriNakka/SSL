SSL files

### Important points
```java
int myInt[][]..[] = new int[size1][size2]..[size3]
##### need to know arra size

Length of rows:
myInt.length;

Length of columns:
myInt[0].length;

for(int i=0; i<arr.length; i++){
  ...
}

##### Dinamically allocate the size, create empty array list first
ArrayList<String> arr = new ArrayList<String>();
ArrayList<ArrayList<String>> = arr = new ArrayList<ArrayList<String>>();

for(int i=0; i<arr.length; i++){
  System.out.println(arr.get[i]);
}
for(DataType IteratorValue: array){
  print IteratorValue;
}

Iterator<String> itType = arr.iterator();
while(itType.hasNext()){
  print(itType.next())
}

try{
  ...
}
catch{
  ...
}
finally{
  .. // need to print with try ot catch block
}
```
